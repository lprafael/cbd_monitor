-- Schema que usa el backend cbd_monitor para auth (sistema.usuarios, etc.).
-- Las tablas se crean ejecutando backend/init_database.py con AUTH_DB_* apuntando a esta BD.
CREATE SCHEMA IF NOT EXISTS sistema;
